import FrontispieceMap from "./FrontispieceMap.astro";

export default {
  title: "Appendix/FrontispieceMap",
  component: FrontispieceMap,
};

/** Hover a region of the plate to preview it; click to pin. */
export const Default = {};
