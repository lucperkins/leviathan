import Navbar from "../theme/components/Navbar.astro";

export default {
  title: "Navigation/Navbar",
  component: Navbar,
};

/** The bar shown below the desk breakpoint. The menu button flips the Alpine `drawer` store; there is no drawer here to open. */
export const Default = {};
