---
/**
 * A collapsible sidebar section. Open/closed state is owned by the Alpine
 * `navSection` component and persisted per `id`. Every section starts closed;
 * the pre-paint script in Layout.astro applies the persisted state before
 * Alpine boots, and the section holding the current page is forced open.
 */
import NavIcon from "./NavIcon.astro";

interface Props {
  id: string;
  title: string;
  open?: boolean;
  /** Nested inside another NavSection: indented, tighter spacing. */
  nested?: boolean;
}
const { id, title, open = false, nested = false } = Astro.props;
---

<details
  class:list={["nav-section group", nested ? "mb-2 ml-2" : "mb-4"]}
  data-nav-section={id}
  open={open}
  x-data={`navSection('${id}')`}
  x-bind:open="open"
  x-on:toggle="open = $el.open"
>
  <summary
    class="flex cursor-pointer list-none items-center justify-between rounded py-1 pr-3 pl-3 text-sm font-semibold text-stone-500 select-none dark:text-stone-400 hover:bg-stone-200 hover:text-stone-700 dark:hover:bg-stone-800 dark:hover:text-stone-300 [&::-webkit-details-marker]:hidden"
  >
    <span class="flex min-w-0 items-center gap-2">
      {!nested && <NavIcon name={id} />}
      <span class="min-w-0">{title}</span>
    </span>
    <svg
      aria-hidden="true"
      viewBox="0 0 16 16"
      width="12"
      height="12"
      fill="none"
      stroke="currentColor"
      stroke-width="1.75"
      stroke-linecap="round"
      stroke-linejoin="round"
      class="ml-2 shrink-0 transition-transform group-open:rotate-90"
    >
      <path d="m6 3 5 5-5 5"></path>
    </svg>
  </summary>
  <div class="mt-1">
    <slot />
  </div>
</details>
